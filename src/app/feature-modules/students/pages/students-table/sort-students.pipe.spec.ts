import { SortStudentsPipe } from './sort-students.pipe';

describe('SortStudentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortStudentsPipe();
    expect(pipe).toBeTruthy();
  });
});
